========================================
匠寵後端服務器 - 簡單啟動說明
========================================

如果無法啟動服務器，請按照以下步驟操作：

【步驟 1】打開終端機
- macOS: 按 Cmd+Space，搜尋「終端機」或「Terminal」

【步驟 2】進入後端目錄
在終端機中輸入：
cd ~/Downloads/code_sandbox_light_c2866122_1766354127/backend

【步驟 3】檢查環境
node 檢查環境.js

【步驟 4】安裝依賴（如果缺少）
npm install

如果 npm install 失敗，嘗試：
npm install --no-save
或
npm install --prefix .

【步驟 5】建立 .env 文件（如果不存在）
複製以下內容到終端機執行：

cat > .env << 'EOF'
PORT=3000
NODE_ENV=development
JWT_SECRET=jiangchong-secret-key-2024
JWT_EXPIRE=7d
DB_PATH=./data/jiangchong.db
ECPAY_MERCHANT_ID=2000132
ECPAY_HASH_KEY=5294y06JbISpM5x9
ECPAY_HASH_IV=v77hoKGq4kWxNNIS
ECPAY_ENV=development
FRONTEND_URL=http://localhost:5500
EOF

【步驟 6】初始化資料庫
npm run init-db

【步驟 7】啟動服務器
npm start

========================================
成功啟動後，您會看到：
========================================

✅ 已連接到 SQLite 資料庫
✅ 資料庫表結構初始化完成
🚀 匠寵後端服務器已啟動
📍 服務地址: http://localhost:3000

========================================
測試服務器
========================================

在瀏覽器中打開：
http://localhost:3000/health

應該看到 JSON 回應。

========================================
常見問題
========================================

Q: npm install 權限錯誤？
A: 嘗試使用 npm install --no-save

Q: 端口被占用？
A: 編輯 .env 文件，將 PORT=3000 改為 PORT=3001

Q: 找不到模組？
A: 執行 npm install 重新安裝所有依賴

Q: 資料庫錯誤？
A: 刪除 data/jiangchong.db 文件，然後執行 npm run init-db

========================================
需要更多幫助？
========================================

查看以下文件：
- 快速啟動.md
- 故障排除.md
- README.md

