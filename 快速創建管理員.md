# 🎯 快速開始 - 創建管理員帳號

## 📍 你的專案位置
```
/Users/ming/Desktop/keyboard clicking/p.et/ 網站
```

---

## ⚡ 最快方法（2 分鐘）

### **步驟 1：生成密碼**

打開終端機，複製貼上：

```bash
cd "/Users/ming/Desktop/keyboard clicking/p.et/ 網站/backend"
node -e "const bcrypt = require('bcryptjs'); bcrypt.hash('Admin@123456', 10, (err, hash) => console.log(hash));"
```

📋 **複製輸出的密碼**

---

### **步驟 2：在 Railway 執行 SQL**

1. 打開：https://railway.app
2. 選專案 → **Postgres** → **Query**
3. 貼上 SQL（替換密碼）：

```sql
INSERT INTO users (
    id, email, password, name, "memberLevel", status, 
    "registeredAt", "createdAt", "updatedAt",
    "totalOrders", "totalSpent", "quizCompleted"
) VALUES (
    'admin-' || FLOOR(RANDOM() * 1000000)::TEXT,
    'admin@jiangchong.com',
    '你的加密密碼',  -- ⚠️ 貼這裡
    '匠寵管理員',
    'admin',
    'active',
    EXTRACT(EPOCH FROM NOW()) * 1000,
    EXTRACT(EPOCH FROM NOW()) * 1000,
    EXTRACT(EPOCH FROM NOW()) * 1000,
    0, 0, 0
);
```

4. 點擊 **Run Query**

✅ **完成！**

---

## 🌐 登入後台

```
https://aa89976566.github.io/p.et-taiwan/admin-login.html
```

**登入資訊：**
```
Email:    admin@jiangchong.com
密碼:     Admin@123456
```

---

## 🔄 其他方法

### **方法 A：一鍵腳本**

```bash
cd "/Users/ming/Desktop/keyboard clicking/p.et/ 網站"
./create-admin.sh
```

### **方法 B：使用 npx（不需要權限）**

```bash
cd "/Users/ming/Desktop/keyboard clicking/p.et/ 網站/backend"
npx @railway/cli login
npx @railway/cli link
npx @railway/cli run node scripts/create-admin.js
```

### **方法 C：使用 sudo**

```bash
sudo npm install -g @railway/cli
cd "/Users/ming/Desktop/keyboard clicking/p.et/ 網站/backend"
railway login
railway link
railway run node scripts/create-admin.js
```

---

## 🆘 常見問題

### Q: bcrypt 報錯？

```bash
cd "/Users/ming/Desktop/keyboard clicking/p.et/ 網站/backend"
npm install
```

### Q: 忘記密碼？

在 Railway Query 執行：
```sql
UPDATE users SET password = '新的加密密碼' WHERE email = 'admin@jiangchong.com';
```

### Q: 升級現有帳號為管理員？

在 Railway Query 執行：
```sql
UPDATE users SET "memberLevel" = 'admin' WHERE email = '你的Email';
```

---

## ✅ 完成後做什麼？

1. ✅ 登入後台
2. ✅ 修改密碼
3. ✅ 添加商品
4. ✅ 測試功能

---

**🎉 就這麼簡單！**
