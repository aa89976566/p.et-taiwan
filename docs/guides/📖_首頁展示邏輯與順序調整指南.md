# 📖 首頁展示邏輯與順序調整指南

**更新日期**: 2024-12-21

---

## 🎯 三種調整方式

### 方式 1: 使用後台管理頁面（推薦）⭐

**最簡單的方式，圖形化界面操作**

#### 步驟：

1. **打開後台管理頁面**
   ```
   開啟: admin-homepage.html
   ```

2. **調整 Section 順序**
   - 目前後台頁面有拖拽功能（即將推出）
   - 或使用「編輯」按鈕調整 `order` 值

3. **開啟/關閉 Section**
   - 找到要控制的 Section 卡片
   - 點擊右上角的開關按鈕
   - ✅ = 顯示，❌ = 隱藏

4. **編輯 Section 內容**
   - 點擊藍色「編輯」按鈕
   - 修改標題、副標題、內容等
   - 點擊「儲存變更」

5. **發布更新**
   - 點擊頂部「發布更新」按鈕
   - ✅ 前台立即生效

---

### 方式 2: 使用瀏覽器 Console（快速調整順序）

**適合快速調整順序，無需打開後台**

#### 步驟：

1. **打開首頁**
   ```
   開啟: index.html
   ```

2. **打開瀏覽器開發者工具**
   - 按 `F12` 或 `Cmd+Option+I` (Mac) / `Ctrl+Shift+I` (Windows)
   - 切換到「Console」標籤

3. **調整 Section 順序**

```javascript
// 範例：將測驗提前到第2位
window.HomepageConfig.reorderSections([
    'hero',         // 第1個：Hero 主視覺
    'quiz',         // 第2個：智能測驗（提前）
    'products',     // 第3個：產品系列
    'subscription', // 第4個：訂閱服務
    'jar-exchange', // 第5個：換罐計畫
    'about',        // 第6個：關於我們
    'crowdfunding'  // 第7個：集資項目
]);

// 發布變更
window.HomepageConfig.publishHomepageChanges();

// 重新載入頁面查看效果
location.reload();
```

#### 其他常用操作：

```javascript
// 關閉某個 Section
window.HomepageConfig.toggleSection('crowdfunding'); // 關閉集資項目
window.HomepageConfig.publishHomepageChanges();
location.reload();

// 開啟某個 Section
window.HomepageConfig.updateSectionConfig('crowdfunding', {
    enabled: true
});
window.HomepageConfig.publishHomepageChanges();
location.reload();

// 查看當前配置
const config = window.HomepageConfig.getHomepageConfig();
console.log(config);
```

---

### 方式 3: 直接修改配置文件（進階）

**適合需要永久修改或批量調整**

#### 步驟：

1. **打開配置文件**
   ```
   文件: js/homepage-config.js
   ```

2. **修改 Section 順序**

找到 `DEFAULT_HOMEPAGE_CONFIG` 對象，修改 `order` 值：

```javascript
const DEFAULT_HOMEPAGE_CONFIG = {
    sections: [
        {
            id: 'hero',
            name: 'Hero 主視覺',
            enabled: true,
            order: 1,  // ← 修改這裡的數字來調整順序
            // ...
        },
        {
            id: 'quiz',
            name: '智能測驗',
            enabled: true,
            order: 2,  // ← 改為 2，會顯示在第2位
            // ...
        },
        {
            id: 'products',
            name: '產品系列',
            enabled: true,
            order: 3,  // ← 改為 3，會顯示在第3位
            // ...
        },
        // ... 其他 sections
    ]
};
```

3. **開啟/關閉 Section**

修改 `enabled` 值：
```javascript
{
    id: 'crowdfunding',
    name: '集資項目',
    enabled: false,  // ← false = 隱藏, true = 顯示
    order: 5,
    // ...
}
```

4. **修改 Section 內容**

修改 `content` 對象：
```javascript
{
    id: 'hero',
    name: 'Hero 主視覺',
    enabled: true,
    order: 1,
    content: {
        title: '你的新標題',        // ← 修改標題
        subtitle: '你的新副標題',   // ← 修改副標題
        ctaText: '立即選購',        // ← 修改按鈕文字
        ctaLink: '#products',      // ← 修改按鈕連結
        bgImage: ''                // ← 修改背景圖片
    }
}
```

5. **清除 localStorage（讓新配置生效）**

在瀏覽器 Console 執行：
```javascript
localStorage.removeItem('homepageConfig');
location.reload();
```

---

## 📋 可調整的 Section 列表

| Section ID | 名稱 | 預設順序 | 預設狀態 | 說明 |
|-----------|------|---------|---------|------|
| `hero` | Hero 主視覺 | 1 | ✅ 啟用 | 首頁橫幅，建議保留 |
| `products` | 產品系列 | 2 | ✅ 啟用 | 三大產品線展示 |
| `quiz` | 智能測驗 | 3 | ✅ 啟用 | 測驗系統 |
| `subscription` | 訂閱服務 | 4 | ✅ 啟用 | 訂閱方案 |
| `crowdfunding` | 集資項目 | 5 | ❌ 關閉 | 嘖嘖嘖集資 |
| `jar-exchange` | 換罐計畫 | 6 | ✅ 啟用 | 環保計畫 |
| `about` | 關於我們 | 7 | ✅ 啟用 | 公司介紹 |

---

## 🎨 常見調整範例

### 範例 1: 將測驗提前到第2位

**使用 Console：**
```javascript
window.HomepageConfig.reorderSections([
    'hero', 'quiz', 'products', 'subscription', 'jar-exchange', 'about', 'crowdfunding'
]);
window.HomepageConfig.publishHomepageChanges();
location.reload();
```

**使用配置文件：**
修改 `js/homepage-config.js`：
```javascript
{ id: 'hero', order: 1 },
{ id: 'quiz', order: 2 },      // ← 改為 2
{ id: 'products', order: 3 },  // ← 改為 3
// ...
```

---

### 範例 2: 關閉集資項目，開啟換罐計畫

**使用 Console：**
```javascript
// 關閉集資項目
window.HomepageConfig.updateSectionConfig('crowdfunding', { enabled: false });

// 確保換罐計畫開啟
window.HomepageConfig.updateSectionConfig('jar-exchange', { enabled: true });

window.HomepageConfig.publishHomepageChanges();
location.reload();
```

---

### 範例 3: 修改 Hero 標題和按鈕

**使用 Console：**
```javascript
window.HomepageConfig.updateSectionConfig('hero', {
    content: {
        title: '歡迎來到匠寵',
        subtitle: '為你的毛孩提供最好的',
        ctaText: '開始選購',
        ctaLink: '#products'
    }
});
window.HomepageConfig.publishHomepageChanges();
location.reload();
```

---

### 範例 4: 只顯示 Hero、產品、測驗三個 Section

**使用 Console：**
```javascript
const config = window.HomepageConfig.getHomepageConfig();

// 關閉其他 Section
['subscription', 'crowdfunding', 'jar-exchange', 'about'].forEach(id => {
    window.HomepageConfig.updateSectionConfig(id, { enabled: false });
});

window.HomepageConfig.publishHomepageChanges();
location.reload();
```

---

## 🔧 進階：自訂展示邏輯

### 修改產品系列的顯示模式

在 `js/homepage-config.js` 中：

```javascript
{
    id: 'products',
    name: '產品系列',
    enabled: true,
    order: 2,
    content: {
        title: '三大產品系列',
        subtitle: '滿足不同需求',
        showSnacks: true,        // ← 顯示手作零食
        showSubscription: true,  // ← 顯示訂閱服務
        showToys: true,          // ← 顯示益智玩具
        displayMode: 'tabs'     // ← 'tabs', 'grid', 'carousel'
    }
}
```

---

## ⚠️ 重要提醒

### 1. 必須發布才生效
- 修改後記得執行 `publishHomepageChanges()`
- 或點擊後台的「發布更新」按鈕

### 2. 清除快取
如果修改後沒看到效果：
```javascript
// 清除配置快取
localStorage.removeItem('homepageConfig');
location.reload();
```

### 3. Hero 區塊建議保留
- Hero 是首頁的門面
- 建議永遠保持 `enabled: true` 和 `order: 1`

### 4. 測試後再發布
- 先使用「預覽網站」功能
- 確認效果滿意後再發布

---

## 🆘 常見問題

### Q: 修改後前台沒變化？
**A**: 
1. 確認已執行 `publishHomepageChanges()`
2. 清除瀏覽器快取：`localStorage.removeItem('homepageConfig')`
3. 重新載入頁面：`location.reload()`

### Q: 如何恢復預設配置？
**A**: 
```javascript
window.HomepageConfig.resetToDefaultConfig();
window.HomepageConfig.publishHomepageChanges();
location.reload();
```

### Q: 可以新增自訂 Section 嗎？
**A**: 目前支持 7 個預設 Section，自訂功能需要修改 HTML 和配置系統

### Q: Section 順序可以拖拽調整嗎？
**A**: 後台管理頁面正在開發拖拽功能，目前可以使用 Console 或修改配置文件

---

## 📞 需要幫助？

### 查看相關文檔
- `🚀_首頁Section管理快速指南_v11.0_2024-12-21.md` - 詳細操作指南
- `js/homepage-config.js` - 源代碼包含詳細註釋

### 查看後台管理
- `admin-homepage.html` - 圖形化管理界面

---

**更新日期**: 2024-12-21  
**版本**: v1.0

🎉 **開始調整您的首頁展示邏輯吧！**



