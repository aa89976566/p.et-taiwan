{
  "providers": ["node"],
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs_20"]
    },
    "install": {
      "cmds": [
        "cd backend && npm install"
      ]
    },
    "build": {
      "cmds": []
    },
    "start": {
      "cmd": "cd backend && npm start"
    }
  }
}
