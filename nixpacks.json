{
  "providers": [],
  "buildImage": "nixos/nix",
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs_20", "npm-9_x"],
      "nixOverlays": [],
      "nixLibs": []
    },
    "install": {
      "cmds": ["cd backend && npm ci --prefer-offline --no-audit"]
    },
    "build": {
      "cmds": []
    },
    "start": {
      "cmd": "cd backend && npm start"
    }
  }
}
