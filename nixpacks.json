{
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs_20"]
    },
    "install": {
      "cmds": ["cd backend && npm ci --prefer-offline --no-audit"]
    },
    "build": {
      "cmds": []
    },
    "start": {
      "cmd": "cd backend && npm start"
    }
  }
}
